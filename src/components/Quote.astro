---
const { author, title } = Astro.props


---

<aside>
  <slot />
  <div>
    <p><strong>{author || ''}</strong></p>
    <p>{title || ''}</p>
  </div>
</aside>

<style>
  aside {
    background-color: color-mix(in srgb, var(--sl-color-accent-low), var(--sl-color-text-invert));
    padding: 1em 1.5em;
    width: 100%;
    font-size: 1.3em; 
  }
  div {
    text-align: right;
  }
</style>